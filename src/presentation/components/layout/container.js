import styled from 'styled-components';

import { Flex } from '@components';

const Container = styled(Flex).attrs({
  justifyContent: ['flex-start', 'flext-start', 'center'],
})``;

export default Container;
