import styled from 'styled-components';

import { Flex } from '@components';

const Container = styled(Flex).attrs({
  flexDirection: 'column',
  alignItems: ['flex-start', 'flext-start', 'center', 'center'],
  width: 1,
})``;

export default Container;
